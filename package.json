{
  "name": "leonard-victoria-monorepo",
  "private": true,
  "workspaces": [
    "react-app",
    "server"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace=react-app"
  }
}
